#include<list>
#include "Util.h"
#include "PreProcessing.h"

void PreProcessing::orderByAdjacencies(Util::VertexType Vet[]){
   // vet.sort([](const Util::VertexType& a, const Util::VertexType& b) { return a.l < b.l; });
}
